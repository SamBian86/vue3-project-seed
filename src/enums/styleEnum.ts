export enum StyleEnum {
  // 自定义组件样式
  // element-plus icon的size配置
  ICON_SIZE = 20,
  // icon 颜色
  ICON_DEFAULT_COLOR = "rgba(255,255,255,0.6)",
  // 全局row gutter值
  ROW_GUTTER = 4,
  // 全局col xs值
  COL_XS = 24,
  // 全局col sm值
  COL_SM = 24,
  // 全局col md值
  COL_MD = 12,
  // 全局col lg值
  COL_LG = 12,
  // 全局col xl值
  COL_XL = 12,
  // 全局按钮大小
  BUTTON_SIZE = 'mini',
  // 全局输入框大小
  INPUT_SIZE = 'mini',
  // 全局form大小
  FORM_SIZE = 'mini',
  // 全局form中label宽度
  FORM_LABEL_WIDTH = 100,
  // 全局skeleton展示时间
  FORM_SKELETON_TIME = 200,
  // 全局tag大小
  TAG_SIZE = 'mini',
  // 全局表单页面openDialog延迟时间
  OPEN_DIALOG_TIME = 100,
  // 全局form switch组件颜色
  FORM_SWITCH_ACTIVE_COLOR = "#409EFF",
  FORM_SWITCH_INACTIVE_COLOR = "#dcdfe6",
}